[pytest]
# Run tests from this directory only — prevents pytest from traversing up
# into animoflow-blender/__init__.py (Blender addon, has relative imports incompatible outside bpy)
testpaths = .
rootdir = .
